{
  "scripts": {
    "start": "node index.js",
    "watch": "nodemon index.js",
    "build": "rm -rf build/* && babel --out-dir=build app",
    "lint": "eslint app"
  },
  "dependencies": {
    "babel": "^6.5.2",
    "babel-core": "^6.22.1",
    "babel-preset-es2015": "^6.22.0",
    "hapi": "^16.1.0",
    "request": "^2.79.0"
  },
  "devDependencies": {
    "babel-cli": "^6.22.2"
  }
}
